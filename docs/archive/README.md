# Archive

Frozen historical documents — the pre-implementation design spec (written
before the project was named Plass) and the completed layout-hardening session
log. Nothing here describes current behavior; see the root docs for that.
